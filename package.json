{
  "name": "typescript-react-template",
  "version": "1.0.0",
  "description": "A barebones React and Typescript UI template project.",
  "main": "src/index.tsx",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rimraf public && copyfiles -f src/index.html public && webpack serve --mode=development",
    "build": "rimraf dist && copyfiles -f src/index.html dist && webpack --mode=production",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.33",
    "@types/react-dom": "^17.0.10",
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.2.6",
    "typescript": "^4.4.4",
    "webpack": "^5.61.0",
    "webpack-cli": "^4.9.1",
    "webpack-dev-server": "^4.4.0"
  }
}
